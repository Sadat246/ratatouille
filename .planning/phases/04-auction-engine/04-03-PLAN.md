---
phase: 04-auction-engine
type: execute
---

<objective>
Finish Phase 4 with seller visibility, push-notification plumbing, and the remaining project bookkeeping so the auction engine is genuinely usable from both sides of the marketplace and recorded as complete in planning state.

Purpose: Close the loop on the phase by giving sellers operational visibility, wiring the pulled-forward outbid push path, and documenting the completed auction system for later payment and fulfillment phases.
Output: Web Push helpers and service-worker handlers, notification subscription and dispatch logic, seller auction and outcome views with cancel controls, and completed Phase 4 roadmap/state/summary artifacts.
</objective>

<execution_context>
~/.codex/get-shit-done/workflows/execute-phase.md
~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Read file: .planning/PROJECT.md
Read file: .planning/ROADMAP.md
Read file: .planning/STATE.md
Read file: .planning/phases/04-auction-engine/04-CONTEXT.md
Read file: .planning/phases/04-auction-engine/04-RESEARCH.md
Read file: public/sw.js
Read file: .env.example
Read file: app/(business)/sell/page.tsx
Read file: components/listing/recent-listings-panel.tsx
Read file: lib/auctions/service.ts
Read file: lib/auctions/queries.ts

**Prior decisions affecting this plan:**

- Outbid push notifications were intentionally pulled forward into Phase 4, so this plan must wire the browser subscription path and server send path even though broader push work still belongs to Phase 8.
- Sellers should be hands-off but informed, which means the seller lane needs live auction and outcome visibility rather than just the Phase 3 listing desk.
- The current deployment is a single Node process, so notification dispatch can happen after committed auction transitions without introducing a separate worker tier for this phase.

**Concerns this plan must cover:**

- Push support must degrade safely when VAPID environment variables are missing instead of breaking auction mutations.
- Seller-facing views need to show live and completed outcomes without re-querying or re-computing auction truth independently from the shared auction service.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Web Push plumbing and hook auction events into notification delivery</name>
  <files>.env.example, lib/push/vapid.ts, lib/push/subscriptions.ts, lib/push/notify.ts, app/api/push/subscribe/route.ts, public/sw.js, lib/auctions/service.ts</files>
  <action>Add optional VAPID-backed Web Push support for authenticated users. Create helpers to read VAPID configuration, persist/update subscriptions, prune invalid endpoints, and send outbid, win, and seller-event notifications after committed auction transitions. Add a subscription route for the UI and update the service worker with `push` and `notificationclick` handlers so background messages render something useful. Keep push delivery post-commit, degrade gracefully when env configuration is absent, and do not make notification failure break auction correctness.</action>
  <verify>`npm run lint` and `npm run build` pass, and the push helpers plus service worker compile with optional VAPID support and post-commit notification dispatch hooks</verify>
  <done>The repo can persist browser push subscriptions and attempt outbid/win/seller notifications without letting notification transport become part of the critical auction transaction</done>
</task>

<task type="auto">
  <name>Task 2: Replace seller placeholder monitoring with live auction and outcome views</name>
  <files>app/(business)/sell/page.tsx, app/(business)/sell/auctions/page.tsx, app/(business)/sell/outcomes/page.tsx, components/auction/seller-auction-board.tsx, components/auction/seller-outcomes-list.tsx, components/listing/recent-listings-panel.tsx, lib/auctions/queries.ts</files>
  <action>Turn the seller lane into a working auction operations surface. Keep the existing listing composer reachable, but add live-auction and outcomes routes that show current bid, bid count, time-left, settlement amounts, and final results from real queries. Surface seller cancellation controls only where the shared auction service allows them, and make recent-listing/status panels reflect the new auction lifecycle states instead of the old Phase 3 assumptions. Do not fork seller-only auction math in the UI; all displayed outcome state should come from server queries built on the shared engine.</action>
  <verify>`npm run lint` and `npm run build` pass, and the seller shell compiles with live auction and outcomes routes backed by real database queries</verify>
  <done>Sellers can monitor active auctions, review ended outcomes with commission breakdowns, and cancel active auctions from a real Phase 4 interface instead of a placeholder status board</done>
</task>

<task type="auto">
  <name>Task 3: Verify the completed auction arc and advance Phase 4 planning state</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/04-auction-engine/04-03-SUMMARY.md</files>
  <action>Run the phase verification commands, confirm the implemented Phase 4 scope is reflected in the roadmap and state files, and write a substantive summary for the final plan. Update the roadmap plan list, phase progress table, current focus, accumulated decisions, and any residual concerns that Phase 5 or Phase 6 should inherit. Do not leave Phase 4 marked as not started once the code is in place, and do not omit the push/scheduler caveats from the summary if they remain environment-dependent.</action>
  <verify>`npm run lint` and `npm run build` pass, and the roadmap/state artifacts show Phase 4 completed with the next phase ready to plan</verify>
  <done>The auction engine phase is documented as complete, the project state is advanced, and later phases have the decisions and caveats they need recorded</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Push subscription and service-worker code compile with optional configuration handling
- [ ] Seller routes render real active-auction and outcome data with cancel controls where allowed
- [ ] Phase 4 roadmap/state artifacts reflect completion and identify the next phase cleanly
</verification>

<success_criteria>

- Outbid/win/seller event notification plumbing exists and degrades safely when push credentials are absent
- The seller lane exposes real active-auction monitoring and ended-outcome visibility
- Phase 4 is recorded as complete with decisions and residual caveats captured for later phases
</success_criteria>

<output>
After completion, create `.planning/phases/04-auction-engine/04-03-SUMMARY.md`:

# Phase 4 Plan 3: Seller Visibility and Push Summary

**Finished the auction engine with seller monitoring, Web Push plumbing, and completed Phase 4 project bookkeeping.**

## Accomplishments

- Added push subscription and delivery plumbing for auction events
- Built seller live-auction and outcomes views
- Advanced the roadmap and project state to the next phase

## Files Created/Modified

- List the push, seller, and planning files touched

## Decisions Made

- Record the chosen push fallback behavior and seller-lane structure

## Issues Encountered

- Record notification, service-worker, or seller-query issues and how they were resolved, or "None"

## Next Step

Phase complete, ready for Phase 5
</output>
