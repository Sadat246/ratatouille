---
phase: 09-error-logging-and-bug-reports
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - tools/*
  - package.json
  - README.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/09-error-logging-and-bug-reports/*.md
autonomous: true
---

<objective>
Finish Phase 9 with the LLM-facing retrieval loop, operator documentation, and planning-state closeout.

Purpose: The phase is only complete when an agent or human can list reports, fetch one full Markdown report from the terminal, and understand how the widget/CLI fit into the current demo workflow.

Output: a repo-local `list`/`get` CLI, README usage docs, final planning summaries/state updates, and a verified end-to-end retrieval flow.
</objective>

<execution_context>
Read file: ~/.codex/get-shit-done/workflows/execute-phase.md
Read file: ~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Read file: .planning/PROJECT.md
Read file: .planning/ROADMAP.md
Read file: .planning/STATE.md
Read file: .planning/phases/09-error-logging-and-bug-reports/09-CONTEXT.md
Read file: .planning/phases/09-error-logging-and-bug-reports/DISCOVERY.md
Read file: README.md
Read file: package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a plain Node CLI with exactly the two Phase 9 verbs</name>
  <files>tools/*, package.json</files>
  <action>Create a repo-local CLI that runs without the Next.js app server and supports exactly two commands: `list` and `get &lt;id&gt;`. `list` should print public report IDs plus one-line summaries in a scan-friendly terminal format. `get` should print the full stored Markdown for a single report. Use existing runtime dependencies and environment loading rather than introducing a new CLI framework or a dashboard companion. Do not add extra verbs such as search, close, or status.</action>
  <verify>Running the CLI with `list` and `get` exits successfully against a configured database and rejects invalid usage with a concise help/error message</verify>
  <done>An LLM or human can retrieve reports directly from the terminal with the exact minimal surface requested for the phase.</done>
</task>

<task type="auto">
  <name>Task 2: Document the widget and CLI loop for local and production investigation</name>
  <files>README.md</files>
  <action>Add a focused Phase 9 section to the README explaining what the widget captures, how the preview/redaction flow works, how to submit a report, and how to use the CLI from the repo or the production VM environment. Keep the docs grounded in this repository's current workflow; avoid inventing a larger support process or in-app admin surface.</action>
  <verify>The README includes accurate `list`/`get` usage and describes how report capture behaves without contradicting the implementation</verify>
  <done>The repo now documents the entire user-report → terminal-investigation loop clearly enough for future operator or LLM use.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the phase end to end and close out planning state</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/09-error-logging-and-bug-reports/*.md</files>
  <action>Run the full verification stack for the implemented work, create the summary for this final plan, and update roadmap/state status so Phase 9 is accurately recorded as implemented. If any verification gap remains, document it explicitly in state instead of pretending the phase is fully proven. Do not overwrite unresolved Phase 6-8 human/UAT notes; Phase 9 should land alongside those existing realities.</action>
  <verify>`npm run lint`, `npm run test`, `npm run build`, and a real CLI `list`/`get` smoke path are all executed and reflected in the final state update</verify>
  <done>Phase 9 is fully documented, verified to the extent available locally, and the planning system reflects the new current position accurately.</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint` succeeds
- [ ] `npm run test` succeeds
- [ ] `npm run build` succeeds
- [ ] The CLI `list` command prints public report IDs + summaries
- [ ] The CLI `get <id>` command prints the stored Markdown for a real report
- [ ] ROADMAP and STATE reflect the executed Phase 9 plans accurately
</verification>

<success_criteria>

- The repo exposes exactly the requested `list` + `get` retrieval loop
- README documents the end-to-end bug-report workflow
- Phase 9 is reflected as implemented in planning state
- Existing earlier-phase UAT caveats remain preserved, not overwritten

</success_criteria>

<output>
After completion, create `.planning/phases/09-error-logging-and-bug-reports/09-03-SUMMARY.md`.
</output>
