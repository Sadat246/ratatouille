---
phase: 09-error-logging-and-bug-reports
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - instrumentation-client.ts
  - components/bug-report/*
  - lib/bug-reports/*
  - app/layout.tsx
  - app/globals.css
  - package.json
  - package-lock.json
  - tests/lib/bug-reports/*.test.ts
autonomous: true
---

<objective>
Ship the user-facing Phase 9 experience: automatic recent-action capture, on-demand screenshot preview with redaction, and an always-visible floating bug-report widget mounted across the whole app.

Purpose: The backend is useless unless users can see what will be sent, trust it, and submit a report from any surface without developers instrumenting every screen manually.

Output: a global client telemetry runtime, a root-mounted floating widget with preview/submit flow, screenshot capture via dynamic html2canvas import, and tests for the pure client-side telemetry helpers.
</objective>

<execution_context>
Read file: ~/.codex/get-shit-done/workflows/execute-phase.md
Read file: ~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Read file: .planning/PROJECT.md
Read file: .planning/ROADMAP.md
Read file: .planning/STATE.md
Read file: .planning/phases/09-error-logging-and-bug-reports/09-CONTEXT.md
Read file: .planning/phases/09-error-logging-and-bug-reports/DISCOVERY.md
Read file: app/layout.tsx
Read file: app/globals.css
Read file: components/shell/shell-frame.tsx
Read file: components/nav/bottom-nav.tsx
Read file: components/pwa/pwa-boot.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a global client telemetry runtime with a rolling action ring buffer</name>
  <files>instrumentation-client.ts, lib/bug-reports/*, tests/lib/bug-reports/*.test.ts</files>
  <action>Create a singleton client runtime that automatically records the last 10 high-signal events: initial page view, router transitions, interactive click/tap intents, form submits, same-origin API outcomes, uncaught client errors, and unhandled rejections. Keep the buffer metadata-only; do not capture field values, request bodies, or verbose framework noise. Use the official Next.js client instrumentation entrypoint for early bootstrap, and keep the runtime idempotent so the widget can also safely reinitialize it on mount.</action>
  <verify>`npm run test` covers ring-buffer truncation, action rendering/sanitization, and duplicate-initialization safety</verify>
  <done>A global recent-action buffer exists without per-component instrumentation, and it records concise, privacy-aware actions automatically.</done>
</task>

<task type="auto">
  <name>Task 2: Add on-demand screenshot capture with explicit redaction and failure-tolerant preview</name>
  <files>lib/bug-reports/*, package.json, package-lock.json</files>
  <action>Add `html2canvas` as an explicit dependency and implement an on-demand screenshot helper that captures only the visible viewport, excludes the widget from the capture, blanks form controls, honors a redaction attribute for high-risk elements, caps scale to keep payload size reasonable, and falls back gracefully if capture fails. Do not load screenshot code eagerly at app startup; use a dynamic import so telemetry remains lightweight.</action>
  <verify>`npm run lint`, `npm run test`, and local type/build validation pass with the new dependency and screenshot helper in place</verify>
  <done>The app can generate a redacted current-screen preview without bloating initial startup cost, and capture failures still allow a useful report flow.</done>
</task>

<task type="auto">
  <name>Task 3: Mount the floating bug-report widget globally and wire the preview/submit flow</name>
  <files>components/bug-report/*, app/layout.tsx, app/globals.css, lib/bug-reports/*</files>
  <action>Create an always-visible bottom-right bubble that expands into a compact support-style card on every route. The card should show the screenshot preview state, the recent-actions list, a short description field, and submit/cancel affordances. Position it high enough to avoid the mobile bottom nav, respect safe-area insets, and keep the design aligned with the existing warm Ratatouille visual system rather than importing a generic SaaS widget look. Submit through the new POST route and reset cleanly on success.</action>
  <verify>`npm run lint`, `npm run build`, and manual local rendering checks show the widget on signed-out and shell routes without obscuring primary navigation</verify>
  <done>The widget is globally mounted, visually integrated, shows the user exactly what will be sent, and submits cleanly through the Phase 9 backend.</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint` succeeds
- [ ] `npm run test` succeeds
- [ ] `npm run build` succeeds
- [ ] The floating bug-report bubble renders on signed-out and signed-in routes
- [ ] The preview flow shows recent actions plus screenshot state before submit
- [ ] The widget does not collide with the mobile bottom nav or safe-area inset
</verification>

<success_criteria>

- Action/outcome telemetry is automatic and global
- Screenshot preview is redacted, on-demand, and failure tolerant
- The bug-report bubble is present on every route without feeling bolted on
- Users see and approve the payload before it is sent

</success_criteria>

<output>
After completion, create `.planning/phases/09-error-logging-and-bug-reports/09-02-SUMMARY.md`.
</output>
