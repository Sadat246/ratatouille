---
phase: 09-error-logging-and-bug-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/schema/bug-reports.ts
  - drizzle/*
  - lib/bug-reports/*
  - app/api/bug-reports/route.ts
  - tests/lib/bug-reports/*.test.ts
autonomous: true
---

<objective>
Create the server-side foundation for Phase 9: immutable bug-report records in Postgres, a Markdown-focused persistence layer, and a public submission endpoint that works for both anonymous and signed-in users.

Purpose: The widget and CLI both depend on a stable backend contract. Reports need a clean public ID, agent-readable Markdown, and privacy-conscious server-side sanitization before any client UI ships.

Output: a new bug-report schema + migration, shared server-side report builders, a Node-runtime POST endpoint, and automated coverage for summary/Markdown generation plus input validation.
</objective>

<execution_context>
Read file: ~/.codex/get-shit-done/workflows/execute-phase.md
Read file: ~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Read file: .planning/PROJECT.md
Read file: .planning/ROADMAP.md
Read file: .planning/STATE.md
Read file: .planning/phases/09-error-logging-and-bug-reports/09-CONTEXT.md
Read file: .planning/phases/09-error-logging-and-bug-reports/DISCOVERY.md
Read file: db/schema/identity.ts
Read file: db/client.ts
Read file: lib/auth/session.ts
Read file: app/api/push/subscribe/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the immutable bug-report storage model and migration</name>
  <files>db/schema/bug-reports.ts, drizzle/*</files>
  <action>Create a dedicated bug-report table that stores an internal UUID plus a short public report ID, optional reporter user reference, reporter role, route/url metadata, one-line summary, reporter description, canonical Markdown text, and the captured screenshot payload. Keep the record immutable after creation; do not introduce status workflows, assignment fields, or dashboard-oriented columns because Phase 9 is intentionally pull-only and CLI-first.</action>
  <verify>`npm run db:generate` creates a new migration cleanly, and the generated SQL matches the intended immutable bug-report shape</verify>
  <done>The schema and migration exist, the row shape is CLI-friendly, and there is no unnecessary triage/status surface.</done>
</task>

<task type="auto">
  <name>Task 2: Build shared report sanitization, public-ID, summary, and Markdown generation helpers</name>
  <files>lib/bug-reports/*, tests/lib/bug-reports/*.test.ts</files>
  <action>Create shared server-side helpers that accept a bounded, sanitized payload from the client and produce: a public report ID, a one-line list summary, and the canonical Markdown body. Bias toward agent readability: clear headers, exact URL/viewport/timestamp context, recent actions rendered as concise bullets, and a screenshot section that references the stored payload without dumping unreadable base64 into CLI output. Strip raw form values, request bodies, or other obvious PII from summaries and rendered action lines.</action>
  <verify>`npm run test` covers summary/Markdown generation, report-ID creation, and redaction behavior for high-risk strings</verify>
  <done>Report-building logic is deterministic, privacy-aware, and returns high-signal Markdown plus a concise list summary.</done>
</task>

<task type="auto">
  <name>Task 3: Expose a public submission endpoint that enriches with session context when available</name>
  <files>app/api/bug-reports/route.ts, lib/bug-reports/*</files>
  <action>Add a Node-runtime POST route that accepts anonymous or signed-in submissions, validates the payload with zod, enriches the report with optional session role/user metadata, writes the immutable row, and returns the public report ID plus list summary. Keep the endpoint narrow: create-only, no admin listing or in-app retrieval API. Reject oversized screenshots or unbounded action arrays so the database surface stays predictable for the later CLI.</action>
  <verify>`npm run lint`, `npm run test`, and a local request against the route handler shape show accepted valid payloads and rejected malformed/oversized ones</verify>
  <done>The backend contract is stable for the widget, supports anonymous and signed-in reporters, and persists agent-readable reports safely.</done>
</task>

</tasks>

<verification>
- [ ] `npm run db:generate` succeeds and produces one new migration for the bug-report table
- [ ] `npm run lint` succeeds
- [ ] `npm run test` succeeds
- [ ] Valid report payloads are accepted and return a public report ID
- [ ] Invalid or oversized payloads are rejected with a clear 4xx response
</verification>

<success_criteria>

- Bug reports persist as immutable Postgres rows with canonical Markdown
- A short public report ID exists for terminal-oriented retrieval
- Submission works for both anonymous and signed-in users
- Server-side summary and Markdown rendering avoid raw PII dumps

</success_criteria>

<output>
After completion, create `.planning/phases/09-error-logging-and-bug-reports/09-01-SUMMARY.md`.
</output>
