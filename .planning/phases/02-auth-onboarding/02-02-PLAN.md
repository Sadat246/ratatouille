---
phase: 02-auth-onboarding
type: execute
---

<objective>
Build the complete consumer lane from role-first sign-in through onboarding completion and handoff into the shopper shell.

Purpose: Prove the consumer side can enter through a dedicated sign-in path, complete the minimum onboarding data reliably, and land in the app with role persisted.
Output: Consumer sign-in route, consumer onboarding UI and server actions, geocoded location capture, and a working redirect into the protected shopper shell.
</objective>

<execution_context>
~/.codex/get-shit-done/workflows/execute-phase.md
~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Read file: .planning/PROJECT.md
Read file: .planning/ROADMAP.md
Read file: .planning/STATE.md
Read file: .planning/phases/02-auth-onboarding/02-CONTEXT.md
Read file: .planning/phases/02-auth-onboarding/02-RESEARCH.md
Read file: .planning/phases/02-auth-onboarding/02-01-SUMMARY.md
Read file: app/page.tsx
Read file: app/(consumer)/shop/page.tsx
Read file: components/shell/shell-frame.tsx
Read file: auth.ts
Read file: lib/auth/session.ts
Read file: lib/auth/onboarding.ts

**Prior decisions affecting this plan:**

- The phase uses one Google provider with route-level role intent, not two OAuth apps.
- The shell remains server-first; onboarding UI may use small client islands, but persistence and authorization stay server-side.
- Consumer onboarding must capture trustworthy location data for later feed and fulfillment work, so browser geolocation is only a convenience path and not the only path.

**Deferred issues being addressed:**

- None beyond the active Phase 2 onboarding scope.

**Concerns being verified:**

- The public role entry needs to feel clean and not leak business UI or dual-role ambiguity into the consumer path.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the public consumer lane into a real role-first auth entry</name>
  <files>app/page.tsx, app/(auth)/signin/consumer/page.tsx, components/auth/google-intent-button.tsx, components/auth/auth-lane-card.tsx, lib/auth/intent.ts</files>
  <action>Replace the Phase 1 consumer preview entry with a real consumer sign-in lane. Add a dedicated consumer sign-in route that starts the Google flow, preserves consumer intent, and routes successful auth into the consumer onboarding surface. If a signed-in account already belongs to the opposite role, present a clear “use a different Google account” path instead of silently switching modes. Do not add a generic all-in-one sign-in page or a role toggle after login, because the phase context explicitly wants the entry point itself to define the role.</action>
  <verify>`npm run build` passes and a local request flow can resolve the consumer sign-in route plus the Auth.js handler without route errors</verify>
  <done>The landing page and dedicated consumer auth route create a clean consumer-first sign-in path with explicit role intent and opposite-role conflict handling</done>
</task>

<task type="auto">
  <name>Task 2: Implement the consumer onboarding wizard with validated location capture</name>
  <files>app/(onboarding)/onboarding/consumer/page.tsx, app/(onboarding)/onboarding/consumer/actions.ts, components/onboarding/consumer-wizard.tsx, components/onboarding/progress-steps.tsx, lib/geo/mapbox.ts, lib/geo/normalize.ts, lib/validation/onboarding.ts</files>
  <action>Build the multi-step consumer onboarding flow described in the phase context: location step first, then display-name confirmation, then delivery address capture. Use `zod` for server-side validation and only a small client wizard shell for step state. Implement confirmed location capture with an address or postal-code search path plus optional browser geolocation as a convenience shortcut, then persist normalized coordinates and address metadata server-side. Choose Mapbox Search for the demo-friendly free-tier path, but keep the geocoding access behind a small adapter so later phases are not coupled to raw vendor response shapes. Do not rely on plain free-text geocoding or on browser geolocation alone, because later distance and fulfillment features need reliable persisted coordinates.</action>
  <verify>`npm run lint` and `npm run build` pass, and the onboarding action can persist a completed consumer profile from valid form input without type or schema errors</verify>
  <done>A newly authenticated consumer can complete a guided onboarding wizard, store reliable location data, mark onboarding complete, and be redirected into the shopper shell</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] The public consumer sign-in route renders and starts the Google auth flow
- [ ] Valid consumer onboarding input persists and redirects to the shopper shell
- [ ] Opposite-role accounts are blocked from entering the consumer lane
</verification>

<success_criteria>

- The consumer side has a clear role-first entry from the public landing page
- Consumer onboarding persists trustworthy location and delivery data
- The consumer role is stored once and not treated as a toggleable mode
- The shopper shell becomes a real post-auth destination instead of a public preview
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-onboarding/02-02-SUMMARY.md`:

# Phase 2 Plan 2: Consumer Lane Summary

**Turned the consumer path into a real sign-in and onboarding flow with persisted location data.**

## Accomplishments

- Added the role-first consumer auth entry
- Built the consumer onboarding wizard and persistence flow
- Landed completed consumer users in the shopper shell

## Files Created/Modified

- List the landing, auth, onboarding, geo, and validation files touched

## Decisions Made

- Record any consumer-lane UX or geocoding implementation choices that matter later

## Issues Encountered

- Record validation, auth-flow, or geocoding issues and how they were resolved, or "None"

## Next Step

Ready for `02-03-PLAN.md`
</output>
