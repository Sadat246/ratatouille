---
phase: 02-auth-onboarding
type: execute
---

<objective>
Build the business lane from dedicated sign-in through store onboarding completion so sellers land in the recovery shell with trustworthy store data.

Purpose: Make the business side feel just as role-clean as the consumer side while collecting the address, contact, and store metadata later phases will depend on.
Output: Business sign-in route, business onboarding wizard, transactional business/profile creation, geocoded storefront data, and redirect into the seller shell.
</objective>

<execution_context>
~/.codex/get-shit-done/workflows/execute-phase.md
~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Read file: .planning/PROJECT.md
Read file: .planning/ROADMAP.md
Read file: .planning/STATE.md
Read file: .planning/phases/02-auth-onboarding/02-CONTEXT.md
Read file: .planning/phases/02-auth-onboarding/02-RESEARCH.md
Read file: .planning/phases/02-auth-onboarding/02-01-SUMMARY.md
Read file: .planning/phases/02-auth-onboarding/02-02-SUMMARY.md
Read file: app/page.tsx
Read file: app/(business)/sell/page.tsx
Read file: db/schema/businesses.ts
Read file: lib/auth/onboarding.ts
Read file: lib/geo/mapbox.ts

**Prior decisions affecting this plan:**

- Separate Google accounts per role is an intentional simplification for v1; do not add role switching or account linking.
- Business onboarding data is foundational for later feed and fulfillment phases, so address capture must be confirmed and geocoded before completion.
- The existing business and membership tables from Phase 1 are the source of truth; onboarding should populate them transactionally instead of inventing duplicate seller-profile tables.

**Deferred issues being addressed:**

- None beyond the active Phase 2 onboarding scope.

**Concerns being verified:**

- Storefront data written here must be reliable enough that later phases can use it without a cleanup migration.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the business auth entry and role-conflict handling</name>
  <files>app/page.tsx, app/(auth)/signin/business/page.tsx, components/auth/google-intent-button.tsx, components/auth/auth-lane-card.tsx, lib/auth/intent.ts</files>
  <action>Add the dedicated business sign-in route and wire the landing page’s seller lane into it. Reuse the shared intent-aware auth entry components from the consumer plan where that reduces duplication, but keep the copy, redirects, and post-login destination business-specific. If an authenticated user already belongs to the consumer role, block the path with an explicit “use a different Google account” message rather than mutating their role. Do not add business-only OAuth scopes or a second provider configuration, because the product wants one provider plus role intent, not auth fragmentation.</action>
  <verify>`npm run build` passes and the business sign-in route resolves without routing or auth-handler errors</verify>
  <done>The public seller lane is a real business sign-in entry that preserves role intent and handles opposite-role conflicts cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Implement the business onboarding wizard and transactional store creation</name>
  <files>app/(onboarding)/onboarding/business/page.tsx, app/(onboarding)/onboarding/business/actions.ts, components/onboarding/business-wizard.tsx, components/onboarding/progress-steps.tsx, lib/geo/mapbox.ts, lib/geo/normalize.ts, lib/validation/onboarding.ts, db/schema/businesses.ts</files>
  <action>Build the multi-step business onboarding flow with progress UI and the exact high-value fields from the phase context: store name, confirmed storefront address, pickup contact details, and operating or pickup hours. On completion, geocode the confirmed address server-side, create or update the business row, create the owner membership row, mark the user as a business account with onboarding complete, and redirect into the seller shell. Generate slugs deterministically and resolve collisions without manual intervention. Avoid free-text-only address capture, and avoid writing business creation in disconnected steps, because later listing and fulfillment work assumes this data is atomically correct when onboarding finishes.</action>
  <verify>`npm run lint` and `npm run build` pass, and valid business onboarding input writes the business and membership records without schema or transaction errors</verify>
  <done>A newly authenticated business user can complete a guided onboarding wizard, persist a geocoded storefront plus owner membership, and enter the seller shell with onboarding marked complete</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] The public business sign-in route renders and starts the Google auth flow
- [ ] Valid business onboarding input creates the business and owner membership records
- [ ] Opposite-role accounts are blocked from entering the business lane
</verification>

<success_criteria>

- The business side has a dedicated sign-in path that feels separate from the consumer side
- Business onboarding writes geocoded storefront data and owner membership in one flow
- Seller users land in the business shell only after onboarding completes
- The stored business data is reliable enough for listing, feed, and fulfillment phases to trust
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-onboarding/02-03-SUMMARY.md`:

# Phase 2 Plan 3: Business Lane Summary

**Turned the business path into a real sign-in and onboarding flow with transactional storefront creation.**

## Accomplishments

- Added the role-first business auth entry
- Built the business onboarding wizard and persistence flow
- Landed completed business users in the seller shell

## Files Created/Modified

- List the landing, auth, onboarding, schema, and geo files touched

## Decisions Made

- Record the slugging, address, and business-ownership choices that constrain later phases

## Issues Encountered

- Record auth-flow, geocoding, or transaction issues and how they were resolved, or "None"

## Next Step

Ready for `02-04-PLAN.md`
</output>
