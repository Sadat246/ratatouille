---
phase: 06-payments
plan: 06
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05"]
files_modified:
  - README.md
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-10, D-12]

must_haves:
  truths:
    - "README contains a 'Demo walk-through' section at the end that a reviewer can follow end-to-end: first-bid SetupIntent with card 4242, natural auction close → off-session PI → captured, buyout path, and two failure paths (insufficient_funds → fallback bidder, authentication_required → fail-fast)"
    - "Walk-through anchors to existing code: references `runFallbackBidderLoop`, `chargeBidderOffSession`, `chargeBuyout`, `off_session=true`, idempotency key pattern `pi:{settlementId}:attempt:{n}`, PI metadata keys, and `npm run db:studio` for DB verification"
    - "Seller outcomes page (/sell/outcomes) is explicitly called out as the verification surface for commission split — no new UI needed (existing SellerOutcomesList already renders gross, fee, net, and paymentStatus)"
    - "No new code paths, no new env vars, no new DB columns — docs-only"
  artifacts:
    - path: "README.md"
      provides: "End-to-end Phase 6 demo walk-through — happy path, failure paths, dashboard verification"
      contains: "## Demo walk-through"
  key_links:
    - from: "README.md::Demo walk-through"
      to: "lib/payments/auction-trigger.ts / lib/payments/fallback.ts / lib/payments/payment-intents.ts"
      via: "symbols referenced by name so a reviewer can grep from README to implementation"
      pattern: "runFallbackBidderLoop|chargeBidderOffSession|chargeBuyout"
---

<objective>
Close Phase 6 with a single cross-cutting deliverable: an end-to-end "Demo walk-through" section appended to `README.md`. A reviewer who reads it must be able to (a) reach a captured test-mode PaymentIntent for a winning bid, (b) observe the fallback bidder loop after a triggered failure, (c) buyout an auction, and (d) confirm the 10% / 90% commission split on `/sell/outcomes` — without asking the planner a single follow-up question.

Why this is its own plan: all five prior plans (06-01 deps/schema, 06-02 server services, 06-03 webhook, 06-04 first-bid UI, 06-05 post-commit trigger) must land before this document is accurate, so it ships in Wave 4 with `depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05"]`. Splitting it out also keeps 06-05 scoped to code.

Existing coverage that this plan does NOT duplicate:
- Stripe CLI + test cards → Plan 06-01 Task 7 (`## Stripe webhooks (local development)` README section).
- Env var list → Plan 06-01 Task 2 (`.env.example`).
- Seller-side visibility of commission split → already shipped: `app/(business)/sell/outcomes/page.tsx` renders `grossAmountCents`, `platformFeeCents`, `sellerNetAmountCents`, `settlementStatus`, `settlementPaymentStatus` via `components/auction/seller-outcomes-list.tsx` from the `getSellerOutcomes` query in `lib/auctions/queries.ts`. No UI work required.

Output: One README section (~90 lines markdown). No code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-payments/06-CONTEXT.md
@.planning/phases/06-payments/06-VALIDATION.md
@.planning/phases/06-payments/06-01-SUMMARY.md
@.planning/phases/06-payments/06-02-SUMMARY.md
@.planning/phases/06-payments/06-03-SUMMARY.md
@.planning/phases/06-payments/06-04-SUMMARY.md
@.planning/phases/06-payments/06-05-SUMMARY.md

<existing_surfaces>
These are already shipped or shipped by earlier plans in this phase; the README walk-through MUST match what a reviewer will actually see.

- Consumer bid surface: `components/auction/auction-bid-panel.tsx` — shows `<StripeCardSetup>` on first bid when `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` is set and the user has no saved card; otherwise shows the existing bid form. `<MockCardPanel>` returns `null` when the publishable key is set.
- Buyout entry: `buyNowAction` server action in the listing page (existing from Phase 4) — hits `buyoutAuction` in `lib/auctions/service.ts`.
- Settlement visibility: `/sell/outcomes` renders each closed auction with `status`, `paymentStatus`, `grossAmountCents`, `platformFeeCents`, `sellerNetAmountCents` (see `components/auction/seller-outcomes-list.tsx`).
- Trigger wiring (Plan 06-05): `triggerAuctionPaymentIfCloseResult` fires in `buyoutAuction` (on-session `chargeBuyout`), `sweepOverdueAuctions`, and `refreshAuctionIfOverdue` (both off-session `chargeBidderOffSession`, attemptNumber=1) — all POST-commit.
- Webhook terminal states (Plan 06-03): `payment_intent.succeeded` → `paymentStatus='captured'` + fulfillment row insert; `payment_intent.payment_failed` → calls `runFallbackBidderLoop`; `payment_intent.canceled` → `paymentStatus='failed'`, `status='voided'`.
</existing_surfaces>

<validation_source>
Steps MUST match Plan 06-VALIDATION.md "Manual-Only Verifications" table:

| ID | Summary |
|----|---------|
| MV-1 | First bid triggers SetupIntent + Payment Element; card 4242 attaches |
| MV-2 | Second bid by same user reuses saved card (no modal) |
| MV-3 | Natural auction close fires off-session PI; settlement captured; fulfillment row created |
| MV-4 | Failed winning bid (card 4000 0000 0000 9995) triggers fallback loop to runner-up |
| MV-5 | Buyout fires on-session PI; captured immediately |
| MV-6 | Seller outcomes page shows 10% fee, 90% net |
| MV-7 | `requires_action` card (4000 0027 6000 3184) off-session fails fast (not parked) |
| MV-8 | Duplicate webhook delivery is deduped (no second fulfillment insert) |
</validation_source>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append "Demo walk-through" section to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — must understand current structure and the `## Stripe webhooks (local development)` section appended by Plan 06-01 Task 7)
    - .planning/phases/06-payments/06-CONTEXT.md (D-01..D-13 — decisions to anchor language against, especially D-07 commission = 10%, D-05 buyout is on-session, D-06 fallback loop)
    - .planning/phases/06-payments/06-VALIDATION.md (Manual-Only Verifications table — steps MUST trace 1:1 to MV-1..MV-8)
    - components/auction/auction-bid-panel.tsx lines 1–60 (to confirm the exact user-facing UX path being documented — StripeCardSetup mount conditions)
    - components/auction/mock-card-panel.tsx (to confirm it hides when publishable key is set — the walk-through can tell the reviewer the mock panel disappears)
    - app/(business)/sell/outcomes/page.tsx + components/auction/seller-outcomes-list.tsx (to confirm the verification surface actually shows fee and net)
    - lib/payments/payment-intents.ts (to confirm metadata keys: `settlementId`, `auctionId`, `bidderUserId`, `attemptNumber`, `kind`)
  </read_first>
  <action>
  Append EXACTLY the following section at the end of `README.md`. It must come AFTER the `## Stripe webhooks (local development)` section that Plan 06-01 Task 7 adds. Do NOT modify any existing section. Do NOT add emoji. Do NOT add H1 headings.

  ```markdown

  ## Demo walk-through

  End-to-end Phase 6 happy path, then two failure paths, then what to verify on
  the Stripe Dashboard. Assumes you've completed the setup in the
  `## Stripe webhooks (local development)` section above — `STRIPE_SECRET_KEY`,
  `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`, and `STRIPE_WEBHOOK_SECRET` are in
  `.env.local`, and `stripe listen --forward-to localhost:3000/api/webhooks/stripe`
  is running in a side terminal (the CLI-provided `whsec_...` value overrides
  any dashboard webhook secret while it runs).

  No real money is ever charged — Stripe is in test mode.

  ### Happy path: first bid → auction close → capture

  1. `npm run dev`. Sign in as a fresh consumer via Google OAuth and finish
     onboarding. With `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` set, the mock card
     panel is hidden — the real Stripe flow takes over.
  2. Open any active auction on the consumer feed.
  3. Tap Bid. The inline Stripe Payment Element appears (iframe origin
     `js.stripe.com`). Enter card `4242 4242 4242 4242`, any future expiry,
     any CVC, any ZIP. Tap Save card.
  4. The bid panel unlocks optimistically and submits the original bid. The
     `setup_intent.succeeded` webhook lands within ~1 s and persists
     `stripeCustomerId` + `stripePaymentMethodId` on the consumer profile.
     Verify with `npm run db:studio` — `consumer_profiles` row now carries
     both columns plus `has_mock_card_on_file = true`.
  5. Place another bid on the same (or any other) auction. No card prompt
     appears — the saved card is reused. (Matches decision D-02.)
  6. Wait for the auction to close, or shorten its `ends_at` in the DB. The
     15-second sweep (`sweepOverdueAuctions` in `lib/auctions/service.ts`)
     closes the auction, writes a `settlements` row with
     `payment_status = 'pending_authorization'`, and — strictly AFTER the
     Drizzle transaction commits — calls `triggerAuctionPaymentIfCloseResult`
     which invokes `chargeBidderOffSession` with `off_session: true`,
     `error_on_requires_action: true`, and metadata
     `kind=auction_winner`.
  7. The `payment_intent.succeeded` webhook lands. `settlements.payment_status`
     flips to `captured`, and a `fulfillments` row is inserted with
     `mode = 'pickup'` (from decision D-10). Re-delivery of the same event
     (use `stripe events resend evt_...`) is a no-op — see the
     `stripe_webhook_events` idempotency table.
  8. Sign out, sign in as the seller, open `/sell/outcomes`. The auction is
     listed with sale price, a 10% platform fee, and the seller's net
     (90% of gross). Decision D-07.

  ### Failure path A: insufficient funds → fallback bidder

  1. Seed two consumers. Give the high bidder card `4000 0000 0000 9995`
     (insufficient_funds) via the first-bid flow. Give the runner-up card
     `4242 4242 4242 4242`.
  2. Let the high bidder win the auction.
  3. When the sweep closes the auction, the off-session PaymentIntent for
     the winner fails. The `payment_intent.payment_failed` webhook (and the
     direct call in `triggerAuctionPaymentIfCloseResult`) both invoke
     `runFallbackBidderLoop(settlementId)` — the first to arrive wins;
     the other is a no-op because the settlement lock (`SELECT … FOR UPDATE`)
     inside the loop serializes them.
  4. The loop rewrites `settlements.buyer_user_id` to the runner-up,
     recomputes `gross_amount_cents` / `platform_fee_cents` /
     `seller_net_amount_cents` from the runner-up's bid, and charges them
     off-session with `attemptNumber = 2`. The runner-up's card captures
     and fulfillment is created.
  5. `/sell/outcomes` now reflects the runner-up's bid as the sale price.
     Decision D-06.

  ### Failure path B: authentication required → fail-fast (no 3DS park)

  1. Seed a high bidder with card `4000 0027 6000 3184`
     (authentication_required). Let them win.
  2. When the sweep closes the auction, `chargeBidderOffSession` creates the
     PaymentIntent with `error_on_requires_action: true`. Stripe does NOT
     park the PI in `requires_action` — it fails synchronously with
     `StripeCardError`.
  3. `triggerAuctionPaymentIfCloseResult` catches the `failed` outcome and
     calls `runFallbackBidderLoop` — same as Failure path A. This is the
     core double-charge safety net: a parked PI could be completed by the
     shopper later, after we've already charged the next bidder.
  4. If there is no runner-up with a saved card, the loop marks the listing
     `expired` and the settlement `payment_status='failed'`.

  ### Buyout path

  1. On any active auction, tap Buy now. A PaymentIntent is created
     **on-session** (no `off_session: true`, no
     `error_on_requires_action: true` — 3DS is permitted) with metadata
     `kind=buyout` and idempotency key `pi:buyout:{settlementId}`.
  2. The charge captures immediately while the shopper is on-page.
     `buyoutAuction` closes the auction synchronously; the
     `payment_intent.succeeded` webhook flips the settlement to `captured`
     and creates the fulfillment row within ~1 s.
  3. A buyout whose charge fails is NOT rerouted to runners-up — only the
     person who clicked Buy now gets that seat. The settlement is marked
     `voided` / `failed` (decision D-05).

  ### What to verify on the Stripe Dashboard

  - Every winning-bid PaymentIntent shows `off_session = true` and metadata
    containing `settlementId`, `auctionId`, `bidderUserId`, `attemptNumber`,
    and `kind = auction_winner`.
  - Buyout PaymentIntents carry `kind = buyout` and do NOT have
    `off_session = true`.
  - Retrying a failed attempt with the same `{settlementId, attemptNumber}`
    returns the cached PaymentIntent (idempotency key
    `pi:{settlementId}:attempt:{n}`) rather than creating a duplicate.
  - No live-mode charges exist. If you see one, stop the demo — an env var
    is wrong.
  ```

  Formatting rules:
  - Use a blank line BEFORE the `## Demo walk-through` heading so markdown renders it as a new section.
  - Preserve the trailing newline convention of the existing README (file ends with a final `\n` after the last line).
  - Use fenced code blocks only where shown — do NOT wrap numbered steps in code.
  - Inline code spans (single backticks) only for identifiers: env var names, DB columns, function names, test card numbers, metadata keys, file paths.
  - Card numbers are written with spaces (`4242 4242 4242 4242`) exactly as they appear on stripe.com/docs/testing — do NOT collapse to `4242424242424242`.
  </action>
  <verify>
    <automated>grep -q "^## Demo walk-through$" README.md && grep -q "^### Happy path: first bid → auction close → capture$" README.md && grep -q "^### Failure path A: insufficient funds → fallback bidder$" README.md && grep -q "^### Failure path B: authentication required → fail-fast" README.md && grep -q "^### Buyout path$" README.md && grep -q "^### What to verify on the Stripe Dashboard$" README.md && grep -q "4242 4242 4242 4242" README.md && grep -q "4000 0000 0000 9995" README.md && grep -q "4000 0027 6000 3184" README.md && grep -q "runFallbackBidderLoop" README.md && grep -q "chargeBidderOffSession" README.md && grep -q "triggerAuctionPaymentIfCloseResult" README.md && grep -q "off_session = true" README.md && grep -q "kind = auction_winner" README.md && grep -q "kind = buyout" README.md && grep -q "pi:{settlementId}:attempt:{n}" README.md && grep -q "pi:buyout:{settlementId}" README.md && grep -q "/sell/outcomes" README.md && grep -q "npm run db:studio" README.md && grep -q "stripe_webhook_events" README.md && grep -q "error_on_requires_action: true" README.md && grep -q "10% platform fee" README.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains the literal heading `## Demo walk-through` exactly once
    - The `## Demo walk-through` heading appears AFTER the `## Stripe webhooks (local development)` heading — confirm with `grep -n "^## " README.md` showing `Demo walk-through` as the last `##` heading in the file
    - Contains all four sub-headings: `### Happy path: first bid → auction close → capture`, `### Failure path A: insufficient funds → fallback bidder`, `### Failure path B: authentication required → fail-fast (no 3DS park)`, `### Buyout path`, `### What to verify on the Stripe Dashboard`
    - Contains the three test card numbers with spaces: `4242 4242 4242 4242`, `4000 0000 0000 9995`, `4000 0027 6000 3184`
    - References the functions `runFallbackBidderLoop`, `chargeBidderOffSession`, `triggerAuctionPaymentIfCloseResult`, `sweepOverdueAuctions`, `buyoutAuction` by name
    - Mentions `off_session = true`, `error_on_requires_action: true`, `kind = auction_winner`, `kind = buyout`
    - Mentions both idempotency key patterns: `pi:{settlementId}:attempt:{n}` (winning bid) and `pi:buyout:{settlementId}` (buyout)
    - References the verification surfaces: `/sell/outcomes`, `npm run db:studio`, `stripe_webhook_events` table
    - References decision anchors: `10% platform fee` (D-07), `90% of gross` (D-07), `mode = 'pickup'` (D-10), `Decision D-02`, `D-05`, `D-06`, `D-07`, `D-10`
    - No existing content is changed — `## Stack`, `## Local development`, `## Database workflow`, `## Stripe webhooks (local development)` sections remain byte-identical to their pre-edit state (compare with `git diff --stat README.md` showing only additions)
    - File ends with a newline character
    - No broken markdown: `npx prettier --check README.md` exits 0, or `grep -c '^```' README.md` returns an even number (all fences balanced)
  </acceptance_criteria>
  <done>A reviewer can follow the README from fresh clone → running app → captured test-mode PaymentIntent → verified 10%/90% commission split, plus both failure paths, without reading any PLAN.md or CONTEXT.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published docs → external readers | Any env var names, secret placeholder patterns, or internal URLs shown in README become public when repo is shared |
| Documented test cards → production | Reviewer following the steps with a production key would attempt real charges |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|-----------|
| T-06-24 | ASVS V6 — Information Disclosure | README.md | low | mitigate | Walk-through shows only env var **names** (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`) and never values. No actual secrets, no prod URLs. Test cards are publicly documented on stripe.com/docs/testing — not a disclosure. |
| T-06-25 | ASVS V14 — Production confusion | README.md | medium | mitigate | First paragraph of the walk-through states "No real money is ever charged — Stripe is in test mode." and "If you see a live-mode charge, stop the demo — an env var is wrong." Reader is steered toward test keys. |
| T-06-26 | Misleading docs — stale after code change | README.md | low | accept | This is a text artifact; drift is unavoidable. Plan 06-06 anchors each step to concrete symbols (`runFallbackBidderLoop`, `chargeBidderOffSession`, etc.) so a future grep-based audit (`gsd-docs-update`) can detect mismatches. |

No high-severity threats introduced by this plan. The six non-negotiable high threats (T-06-01 through T-06-06) are mitigated upstream in Plans 06-01 through 06-05; this plan ships docs only and does not alter any boundary.
</threat_model>

<verification>
- `grep -c "^## " README.md` — count increases by exactly 1 (one new H2).
- `grep -n "^## " README.md` — `Demo walk-through` is the LAST H2 in the file.
- `grep -c "^### " README.md` — count increases by exactly 5 (five new H3s under the Demo walk-through).
- `grep -q "## Stripe webhooks (local development)"` — STILL returns true (Plan 06-01 section untouched).
- `grep -q "## Database workflow"` — STILL returns true (pre-existing section untouched).
- `git diff README.md` — shows only additions (`+` lines, no `-` lines) after the existing end of file.
- Manual: open `README.md` in the editor; visually confirm the narrative order Happy → Failure A → Failure B → Buyout → Dashboard; confirm inline code spans render correctly; confirm no broken links.
- Manual smoke: follow the steps in the new section with `stripe listen` running; all seven MV-* items from `06-VALIDATION.md` must succeed.
</verification>

<success_criteria>
- README ends with a new `## Demo walk-through` section containing the five sub-sections and all grep markers in acceptance criteria.
- No existing README content is modified.
- Section traces 1:1 to MV-1..MV-8 in `06-VALIDATION.md`.
- A reviewer can complete the end-to-end demo using only the README (no PLAN, CONTEXT, or RESEARCH files required).
- `git diff --stat README.md` shows only insertions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-payments/06-06-SUMMARY.md`.
</output>
