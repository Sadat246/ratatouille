---
phase: 08-notifications-demo-polish
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - .env.example
  - lib/demo/*
  - app/api/internal/demo/**/route.ts
  - tests/lib/demo/*.test.ts
autonomous: true
---

<objective>
Create deterministic demo-prep services for Phase 8: a tiny ambient seed world, a repeatable hero-auction path, and guarded internal operator endpoints that can drive the walkthrough on cue.

Purpose: The phase context explicitly prioritizes scripted reliability over organic realism. Demo prep must be rerunnable and must not depend on manual SQL or chance timing.

Output: server-only demo services, guarded internal demo endpoints, and automated coverage for idempotent reseed plus trigger behavior.
</objective>

<execution_context>
Read file: ~/.codex/get-shit-done/workflows/execute-phase.md
Read file: ~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Read file: .planning/PROJECT.md
Read file: .planning/ROADMAP.md
Read file: .planning/STATE.md
Read file: .planning/phases/08-notifications-demo-polish/08-CONTEXT.md
Read file: .planning/phases/08-notifications-demo-polish/DISCOVERY.md
Read file: db/schema/identity.ts
Read file: db/schema/businesses.ts
Read file: db/schema/consumers.ts
Read file: db/schema/listings.ts
Read file: db/schema/auctions.ts
Read file: db/schema/payments.ts
Read file: db/schema/fulfillment.ts
Read file: app/(business)/sell/actions.ts
Read file: lib/auctions/service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a server-only demo world service with idempotent reset and scenario prep</name>
  <files>lib/demo/*, tests/lib/demo/*.test.ts</files>
  <action>Create server-only helpers that can rerun safely against the same database: seed a deliberately small ambient world with a few fake businesses and only enough listings to show fresh, active-bidding, ending-soon, and sold states, and prepare a hero auction flow under a real seller business when requested. Use stable prefixes, slugs, and emails plus cleanup or upsert logic so reruns reset instead of duplicating rows. Because authentication is Google-only, do not add a general sign-in bypass for seeded users; ambient fake accounts can exist as data only, while the interactive hero path must stay anchored to a real seller identity or membership.</action>
  <verify>`npm run test`, `npm run lint`</verify>
  <done>A deterministic demo service exists, reruns are idempotent, and tests cover reset plus scenario invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Expose guarded internal demo endpoints for reseed and scripted triggers</name>
  <files>.env.example, app/api/internal/demo/**/route.ts, lib/demo/*</files>
  <action>Add Node-runtime internal endpoints for actions such as ambient reseed, hero-auction prep, competitor outbid injection, ending-soon sweep now, and close sweep now. Protect them with a dedicated server-side demo token and/or explicit seller authorization so the controls are not public. Return structured JSON that can be scripted from local terminals or the production VM. Reuse the real auction, payment, fulfillment, and push services instead of hand-mutating rows into impossible states. Do not require manual SQL, dashboard clicks, or ad hoc psql updates.</action>
  <verify>`npm run test`, `npm run lint`</verify>
  <done>Guarded internal endpoints can prepare and drive the demo flow through real application logic, and the env contract for enabling them is documented.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the demo controls are safe to rerun and do not bypass the real flow</name>
  <files>tests/lib/demo/*.test.ts, lib/demo/*, app/api/internal/demo/**/route.ts</files>
  <action>Add or extend automated coverage for the highest-risk demo-prep behaviors: reseeding does not multiply records on repeated runs, hero-auction prep attaches to the intended seller scope, competitor-bid injection goes through the real auction logic, and the force-close trigger uses the existing close sweep or shared service path rather than a custom settlement shortcut. Avoid magic UUID assumptions that make the tooling brittle across databases.</action>
  <verify>`npm run test`, `npm run lint`</verify>
  <done>The demo-prep layer is covered by tests that prove it is idempotent, scoped, and built on top of real app behaviors.</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint` succeeds
- [ ] `npm run test` succeeds
- [ ] Ambient reseed can run repeatedly without duplicate demo rows accumulating
- [ ] A hero auction can be recreated on demand without manual DB edits
- [ ] Internal triggers reuse existing auction/notification logic instead of direct row mutation shortcuts
</verification>

<success_criteria>

- Repeatable demo prep exists with no manual SQL
- Ambient fake data stays intentionally small and purposeful
- The interactive hero flow remains anchored to real seller/consumer auth paths
- Internal demo triggers are guarded and scriptable from local or VM environments

</success_criteria>

<output>
After completion, create `.planning/phases/08-notifications-demo-polish/08-02-SUMMARY.md`.
</output>
