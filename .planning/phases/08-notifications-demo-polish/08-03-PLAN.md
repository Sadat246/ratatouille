---
phase: 08-notifications-demo-polish
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - app/(business)/sell/demo/page.tsx
  - components/demo/*
  - components/auction/seller-shell.tsx
  - app/(business)/sell/page.tsx
  - app/(consumer)/shop/alerts/page.tsx
autonomous: false
---

<objective>
Build an in-app seller demo control surface so the presenter can drive the scripted walkthrough from the product instead of juggling raw curl commands.

Purpose: Phase 8 is demo polish. If the operator has to remember hidden endpoints or terminal incantations mid-walkthrough, the experience is still brittle even if the backend is correct.

Output: seller-only demo tools UI, guarded navigation that appears only when demo controls are enabled, and a human-verifiable operator flow for preparing and advancing the hero auction.
</objective>

<execution_context>
Read file: ~/.codex/get-shit-done/workflows/execute-phase.md
Read file: ~/.codex/get-shit-done/templates/summary.md
Read file: ~/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
Read file: .planning/PROJECT.md
Read file: .planning/ROADMAP.md
Read file: .planning/STATE.md
Read file: .planning/phases/08-notifications-demo-polish/08-CONTEXT.md
Read file: .planning/phases/08-notifications-demo-polish/DISCOVERY.md
Read file: components/auction/push-opt-in-panel.tsx
Read file: app/(consumer)/shop/alerts/page.tsx
Read file: components/auction/seller-shell.tsx
Read file: app/(business)/sell/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a seller-only demo tools page that wraps the internal demo endpoints</name>
  <files>app/(business)/sell/demo/page.tsx, components/demo/*</files>
  <action>Create a focused operator page for signed-in sellers that can prepare the hero auction, reseed the ambient world, inject the competitor outbid, trigger the ending-soon sweep immediately, and force the close sweep. Surface concise success/error state and the current hero-auction summary so the presenter knows what will happen next. Gate the entire surface behind the demo-controls env flag or token from Plan 08-02 so it cannot appear in a normal run. Do not expose this UI to consumers, and do not build a generic admin dashboard; this page exists only to drive the scripted Phase 8 walkthrough.</action>
  <verify>`npm run lint`, `npm run test`</verify>
  <done>Signed-in sellers can operate the demo flow from one page, and the page disappears when demo controls are disabled.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten navigation so push opt-in and final outcomes are one or two taps away</name>
  <files>components/auction/seller-shell.tsx, app/(business)/sell/page.tsx, app/(consumer)/shop/alerts/page.tsx, components/demo/*</files>
  <action>Add the smallest possible navigation and page-level affordances so the presenter can reach demo tools, seller outcomes, and consumer alert opt-in without hunting. Preserve the established design language; this is not a redesign phase. The work should improve operator ergonomics while keeping the extra controls invisible when demo mode is off. Do not clutter the main navigation with permanent public-facing demo concepts.</action>
  <verify>`npm run lint`, `npm run test`</verify>
  <done>The demo operator flow is obvious when demo mode is enabled and unchanged when it is disabled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Seller-facing demo tools plus tighter navigation to alerts and outcomes</what-built>
  <how-to-verify>
    1. Configure the demo-control env values plus VAPID values locally.
    2. Run: `npm run dev`
    3. Sign in as a seller account with a storefront membership.
    4. Open the seller lane and confirm the demo tools entry appears only with demo mode enabled.
    5. Use the demo tools to prepare the hero auction, inject the competitor outbid, trigger ending-soon, and force close.
    6. Open the consumer alerts surface and confirm push opt-in is still reachable and the operator flow is easy to follow.
    7. Disable the demo-control env flag, refresh, and confirm the extra demo entry disappears.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe what made the operator flow confusing.</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npm run lint` succeeds
- [ ] `npm run test` succeeds
- [ ] Seller demo tools are hidden when demo mode is disabled
- [ ] Demo controls can prepare and advance the hero auction without leaving the app
- [ ] Consumer alert opt-in is still reachable and understandable during the walkthrough
</verification>

<success_criteria>

- The presenter can drive the scripted Phase 8 flow from inside the product
- Demo-only UI stays hidden outside demo mode
- Navigation friction for alerts and outcomes is reduced without redesigning the app
- Human verification confirms the operator flow is usable

</success_criteria>

<output>
After completion, create `.planning/phases/08-notifications-demo-polish/08-03-SUMMARY.md`.
</output>
