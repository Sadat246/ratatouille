---
phase: 08-notifications-demo-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/schema/auctions.ts
  - db/schema/index.ts
  - drizzle/0006_*.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0006_snapshot.json
  - lib/push/notify.ts
  - lib/auctions/sweeper.ts
  - instrumentation.ts
  - tests/lib/push/*.test.ts
autonomous: true
---

<objective>
Complete the missing Phase 8 notification backend: one-shot auction-ending-soon delivery, explicit win plus business-item-sold payloads, and runtime wiring that makes the missing beat fire automatically.

Purpose: Phase 4 already shipped outbid push and the browser plumbing. Phase 8 should finish that existing system rather than replacing it.

Output: migrated auction schema, tested push helpers for ending-soon / won / seller-sold, and a runtime sweep that can fire the missing notification beat without manual DB edits.
</objective>

<execution_context>
Read file: ~/.codex/get-shit-done/workflows/execute-phase.md
Read file: ~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Read file: .planning/PROJECT.md
Read file: .planning/ROADMAP.md
Read file: .planning/STATE.md
Read file: .planning/phases/08-notifications-demo-polish/08-CONTEXT.md
Read file: .planning/phases/08-notifications-demo-polish/DISCOVERY.md
Read file: .planning/phases/04-auction-engine/04-CONTEXT.md
Read file: .planning/phases/04-auction-engine/04-03-SUMMARY.md
Read file: lib/push/notify.ts
Read file: lib/auctions/sweeper.ts
Read file: instrumentation.ts
Read file: public/sw.js
Read file: app/api/push/subscribe/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend auction persistence for ending-soon dedup state</name>
  <files>db/schema/auctions.ts, db/schema/index.ts, drizzle/*</files>
  <action>Add a dedicated auction-level marker for the ending-soon notification beat and generate the next Drizzle migration. Keep the persistence scope narrow: the Phase 8 gap is a single one-shot timing beat, so prefer a focused field on the auction over a broad notification-event subsystem unless execution proves that narrower approach impossible. Do not hand-wave this into application memory; the marker must survive restarts and repeated sweeps.</action>
  <verify>`npm run db:generate`, `npm run lint`</verify>
  <done>The auction schema has explicit persisted state for whether the ending-soon notification already fired, and the migration artifacts are generated.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tested push helpers for ending-soon, consumer won, and business item sold</name>
  <files>lib/push/notify.ts, tests/lib/push/*.test.ts</files>
  <action>Refactor the push helper module so the Phase 8 notification beats are explicit concepts instead of copy spread across ad hoc switch branches. Keep the current `web-push` plus VAPID stack and preserve the existing graceful no-op when push env vars are absent. Add tests around recipient selection, payload shape, and one-shot ending-soon behavior. Do not add browser detection, client-side background notification creation, or a second push vendor; the service worker and server-side sender already form the correct architecture.</action>
  <verify>`npm run test`, `npm run lint`</verify>
  <done>There are clear server-side helpers for ending-soon, consumer win, and seller item-sold notifications, and tests cover the new behavior plus existing outbid expectations.</done>
</task>

<task type="auto">
  <name>Task 3: Wire a runtime ending-soon sweep beside the existing auction close sweep</name>
  <files>lib/auctions/sweeper.ts, instrumentation.ts, lib/push/notify.ts</files>
  <action>Add a server-side sweep that looks for active auctions inside the chosen ending-soon window, sends the notification beat once, and marks the auction as notified. Start it from instrumentation beside the existing close sweep so it runs automatically anywhere the Node runtime boots. Ensure winner and seller-sold notifications remain tied to durable close/buyout state transitions and still happen outside transaction bodies. Do not move this logic into page loads, feed polling, or client timers.</action>
  <verify>`npm run test`, `npm run lint`</verify>
  <done>The runtime automatically evaluates and emits ending-soon notifications, while close-path win and seller-sold notifications still come from the server-side auction flow.</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint` succeeds
- [ ] `npm run test` succeeds
- [ ] Ending-soon notifications emit at most once per auction
- [ ] Existing outbid notifications still work
- [ ] Consumer win and seller item-sold notifications are triggered from the close path, not from UI polling or browser timers
</verification>

<success_criteria>

- All four roadmap notification beats exist in code
- The only new persisted notification state is the minimal state required for ending-soon dedup
- Push still degrades safely when VAPID env vars are absent
- Runtime startup wires both the settlement sweep and the notification sweep

</success_criteria>

<output>
After completion, create `.planning/phases/08-notifications-demo-polish/08-01-SUMMARY.md`.
</output>
