---
phase: 10-desktop-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/shell/shell-frame.tsx
  - components/nav/bottom-nav.tsx
  - components/auction/consumer-shell.tsx
  - components/auction/section-card.tsx
  - components/auction/filter-chip-row.tsx
  - components/auction/feed-client.tsx
  - components/auction/auction-card.tsx
  - components/pwa/install-prompt-banner.tsx
  - app/(consumer)/shop/page.tsx
  - app/(consumer)/shop/bids/page.tsx
  - app/(consumer)/shop/orders/page.tsx
  - app/(consumer)/shop/alerts/page.tsx
autonomous: true
---

<objective>
Create the shared desktop shell foundation for Ratatouille and apply it to the shopper lane so the app reads like a real desktop marketplace instead of a stretched phone layout.

Purpose: Phase 10 needs a responsive chrome model before any page-by-page polish matters. The shopper side is the clearest proof of the new desktop direction because it combines navigation, browsing density, filtering, and install behavior in one lane.

Output: a desktop-aware shared shell that preserves the mobile bottom nav, a shopper home page with an eBay-style large-screen layout (filter rail + denser auction grid), and shopper surfaces whose spacing/section cards scale cleanly on larger breakpoints.
</objective>

<execution_context>
Read file: ~/.codex/get-shit-done/workflows/execute-phase.md
Read file: ~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Read file: .planning/PROJECT.md
Read file: .planning/ROADMAP.md
Read file: .planning/STATE.md
Read file: .planning/phases/10-desktop-app/10-CONTEXT.md
Read file: .planning/phases/10-desktop-app/DISCOVERY.md
Read file: components/shell/shell-frame.tsx
Read file: components/nav/bottom-nav.tsx
Read file: components/auction/consumer-shell.tsx
Read file: components/auction/filter-chip-row.tsx
Read file: components/auction/feed-client.tsx
Read file: components/auction/auction-card.tsx
Read file: components/pwa/install-prompt-banner.tsx
Read file: app/(consumer)/shop/page.tsx
Read file: app/(consumer)/shop/bids/page.tsx
Read file: app/(consumer)/shop/orders/page.tsx
Read file: app/(consumer)/shop/alerts/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add desktop-aware shared shell chrome while preserving the mobile bottom-nav pattern</name>
  <files>components/shell/shell-frame.tsx, components/nav/bottom-nav.tsx, components/auction/consumer-shell.tsx, components/auction/section-card.tsx</files>
  <action>Refactor the shared shell so large screens get a real workspace composition: left-side navigation rail, roomier content column, and a hero/header treatment that no longer caps the product at phone widths. Preserve the existing mobile experience by keeping the bottom navigation as the primary small-screen affordance and avoid creating separate shopper-only shell forks because Phase 10 needs one reusable responsive shell model.</action>
  <verify>`npm run lint` passes and shopper pages still render with bottom navigation on small screens while large screens hide it in favor of a desktop rail</verify>
  <done>Shared shell chrome supports desktop and mobile at the same time, and shopper pages gain larger-screen spacing without breaking the existing mobile path.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the shopper home into a desktop marketplace layout</name>
  <files>components/auction/filter-chip-row.tsx, components/auction/feed-client.tsx, components/auction/auction-card.tsx, components/pwa/install-prompt-banner.tsx, app/(consumer)/shop/page.tsx</files>
  <action>Recompose the shopper home so desktop users get a browse-heavy layout: persistent filter rail or equivalent left-side filter treatment, clearer top-level sort controls, and a denser multi-column auction grid. Keep the current feed query/infinite-scroll behavior intact, and do not introduce new server contracts or duplicate feed logic just for desktop. Reposition the install prompt so it behaves like a desktop-friendly utility instead of a mobile bottom sheet on large screens.</action>
  <verify>`npm run lint` and `npm run build` pass, and the shopper home renders as a single-column mobile feed on small screens but shifts to a filter-plus-grid workspace on large screens</verify>
  <done>Desktop shopper home feels intentionally designed for large screens while mobile browsing behavior remains intact.</done>
</task>

<task type="auto">
  <name>Task 3: Scale the rest of the shopper lane to the new desktop shell</name>
  <files>app/(consumer)/shop/bids/page.tsx, app/(consumer)/shop/orders/page.tsx, app/(consumer)/shop/alerts/page.tsx, components/auction/section-card.tsx</files>
  <action>Adjust shopper lane section composition so My Bids, Orders, and Alerts inherit the desktop shell cleanly: roomier section cards, better metric-card spacing, and content widths that look deliberate on laptops/monitors. Keep the existing route logic and business behavior unchanged; this is layout and presentation work, not a product-flow rewrite.</action>
  <verify>`npm run lint` and `npm run build` pass, and the shopper subpages no longer appear constrained to a phone-width card stack on desktop breakpoints</verify>
  <done>All shopper shell pages read as part of the same desktop product, not just the home feed.</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint` succeeds
- [ ] `npm run build` succeeds
- [ ] Shopper shell pages still present the mobile bottom nav on small screens
- [ ] Shopper home uses a desktop-oriented filter-plus-grid composition on large screens
- [ ] Shopper bids/orders/alerts pages gain larger-screen spacing without changing route behavior
</verification>

<success_criteria>

- Shared shell chrome supports both mobile and desktop use cases
- Shopper desktop experience feels browse-first rather than phone-stretched
- No shopper route logic or data contracts change
- Mobile navigation and install flows remain usable

</success_criteria>

<output>
After completion, create `.planning/phases/10-desktop-app/10-01-SUMMARY.md`.
</output>
