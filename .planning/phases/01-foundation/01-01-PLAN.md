---
phase: 01-foundation
type: execute
---

<objective>
Bootstrap the repository into a clean Next.js + Postgres foundation with the exact tooling this phase needs.

Purpose: Establish the executable base all later phase work depends on without prematurely building auth, listings, payments, or offline complexity.
Output: A runnable Next.js App Router app, Drizzle + Neon database tooling, environment scaffolding, and repeatable local quality checks.
</objective>

<execution_context>
~/.codex/get-shit-done/workflows/execute-phase.md
~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Read file: .planning/PROJECT.md
Read file: .planning/ROADMAP.md
Read file: .planning/phases/01-foundation/01-CONTEXT.md
Read file: .planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the app with the agreed stack and remove starter noise</name>
  <files>package.json, package-lock.json, tsconfig.json, next.config.ts, eslint.config.mjs, postcss.config.mjs, app/layout.tsx, app/page.tsx, app/globals.css, public/, .gitignore</files>
  <action>Initialize the repository in place with the current empty Next.js App Router template using TypeScript, ESLint, Tailwind, no src directory, and the default import alias. Keep Turbopack enabled because this phase is not using webpack-only PWA plugins. Replace any demo/starter content with a minimal neutral shell so later plans can layer real branding on top. Do not use static export and do not leave example marketing copy or placeholder demo assets in place, because both create rework in the shell and deploy pipeline.</action>
  <verify>`npm install`, `npm run lint`, and `npm run build` complete successfully on the fresh scaffold</verify>
  <done>The repo has a clean runnable Next.js foundation with no tutorial/demo residue and all baseline checks passing</done>
</task>

<task type="auto">
  <name>Task 2: Add database tooling, environment scaffolding, and server-only access boundaries</name>
  <files>package.json, .env.example, drizzle.config.ts, db/client.ts, db/schema/index.ts, lib/env.ts</files>
  <action>Install and wire the recommended database stack: Drizzle ORM, Drizzle Kit, and the Neon serverless driver. Standardize on a single `DATABASE_URL` environment variable and add package scripts for schema generation and migration execution. Create a typed environment helper and a server-only database client module so database access stays on the server from day one. Leave the schema index intentionally skeletal in this plan; full table modeling belongs to the next plan. Do not add auth placeholders, Supabase helpers, Prisma, or custom connection-pool wrappers, because the research explicitly recommends a lean Neon + Drizzle start for this phase.</action>
  <verify>`npm run build` still passes and the Drizzle config can be loaded without runtime or TypeScript errors</verify>
  <done>The project has a repeatable local database toolchain, a checked-in environment template, and a server-only database entry point ready for the schema plan</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] The app starts locally with `npm run dev`
- [ ] Database tooling exists for generation and migration commands without any auth or feature-specific scaffolding
</verification>

<success_criteria>

- The repository is a runnable Next.js App Router application
- The recommended Neon + Drizzle stack is installed and wired
- Environment handling is explicit and checked in via an example file
- No static-export configuration, demo content, or premature auth/payment code was introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`:

# Phase 1 Plan 1: Bootstrap Summary

**Established the executable Next.js + Neon + Drizzle foundation for the project.**

## Accomplishments

- Scaffolded the app with the agreed App Router stack
- Added database tooling and environment scaffolding
- Created a server-only database access boundary

## Files Created/Modified

- List the key config, app, and database-tooling files

## Decisions Made

- Note any implementation detail choices that still follow the research recommendation

## Issues Encountered

- Record setup friction and how it was resolved, or "None"

## Next Step

Ready for `01-02-PLAN.md`
</output>
