---
phase: 01-foundation
type: execute
---

<objective>
Create the full core data model and initial migration set that later phases can build on without structural rework.

Purpose: Lock in the domain backbone early so auction, settlement, and fulfillment features can layer on top of stable tables instead of redesigning the schema mid-sprint.
Output: Complete Drizzle schema definitions, generated initial SQL migrations, and a verified migration path for local and hosted Postgres.
</objective>

<execution_context>
~/.codex/get-shit-done/workflows/execute-phase.md
~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Read file: .planning/PROJECT.md
Read file: .planning/ROADMAP.md
Read file: .planning/phases/01-foundation/01-CONTEXT.md
Read file: .planning/phases/01-foundation/01-RESEARCH.md
Read file: package.json
Read file: drizzle.config.ts
Read file: db/client.ts
Read file: db/schema/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model identity, business, and listing foundations in Drizzle</name>
  <files>db/schema/index.ts, db/schema/identity.ts, db/schema/businesses.ts, db/schema/listings.ts</files>
  <action>Create the first half of the domain model with explicit Postgres types, indexes, and enums. Include at minimum: `users`, `businesses`, `business_memberships`, `listings`, and `listing_images`. Use UUID primary keys, timestamptz audit fields, and integer cents for all money fields. Keep user role state minimal and nullable so Phase 2 can own the actual onboarding flow, but include enough structure that a user can later act as a business member or a consumer without a table rewrite. Model listing media as a dedicated table rather than arrays or JSON blobs so the product / seal / expiry image flow in Phase 3 stays queryable and extensible. Do not use decimals for money, and do not collapse business and user concepts into one table, because both choices create avoidable migration churn later.</action>
  <verify>`npm run build` passes with the new schema modules imported through the shared schema index</verify>
  <done>The schema now includes the identity, business, listing, and listing-image layers with explicit relationships and forward-compatible field choices</done>
</task>

<task type="auto">
  <name>Task 2: Model auction, bidding, settlement, and fulfillment tables with lifecycle-safe statuses</name>
  <files>db/schema/index.ts, db/schema/auctions.ts, db/schema/payments.ts, db/schema/fulfillment.ts</files>
  <action>Create the second half of the domain model with at minimum: `auctions`, `bids`, `settlements`, and `fulfillments`. Put lifecycle state into enums instead of scattered booleans so later plans can express transitions clearly. Ensure the schema can represent reserve pricing, buyout, winner selection, platform fee modeling, payment state, pickup codes, and future Uber Direct references without table redesign. Add unique constraints and indexes where later plans will depend on them, such as one auction per listing, bid ordering by auction and creation time, and one fulfillment record per settlement. Avoid putting payment status directly on the bid or using a single catch-all status field for everything, because the roadmap already separates auction outcome, settlement, and fulfillment concerns.</action>
  <verify>The full schema compiles and the generated TypeScript types expose all core domain tables and enums</verify>
  <done>The complete Phase 1 domain backbone exists in code with clear relationships and lifecycle-specific status modeling</done>
</task>

<task type="auto">
  <name>Task 3: Generate and validate the initial migration set</name>
  <files>drizzle/, db/schema/, package.json</files>
  <action>Use Drizzle Kit to generate the initial SQL migration set from the new schema and ensure the package scripts can apply it consistently. Keep the migration artifacts checked in and reviewable; do not rely on ad hoc SQL pasted into terminals. If a helper script is needed to run migrations in a controlled way, add it now. Validate that the initial migration can be applied cleanly to a fresh database and that repeated runs do not imply hidden drift. Do not use schema push as the canonical workflow for this project, because this phase needs durable migration artifacts, not one-off state mutation.</action>
  <verify>`npm run db:generate` creates migration files and the migration command applies them successfully to a fresh database target</verify>
  <done>Initial schema SQL is checked in, repeatable, and proven to apply cleanly</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `npm run build` passes
- [ ] The schema compiles without type errors
- [ ] Initial migration artifacts are generated and checked in
- [ ] A fresh database can be migrated cleanly with the committed workflow
</verification>

<success_criteria>

- All core tables required by the phase context exist in Drizzle
- Money, timestamps, and status lifecycles follow the project-wide conventions
- The migration workflow is artifact-based and repeatable
- Later phases can add auth, listing capture, auctions, payments, and fulfillment logic without schema redesign
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Schema Summary

**Created the full core marketplace schema and verified the initial migration path.**

## Accomplishments

- Added the complete core domain model
- Generated and checked in the initial migration set
- Verified the migration workflow against a fresh database

## Files Created/Modified

- List the schema modules, migration artifacts, and any supporting scripts

## Decisions Made

- Record important data-model choices such as money representation or status enums

## Issues Encountered

- Record migration or constraint issues and their resolution, or "None"

## Next Step

Ready for `01-03-PLAN.md`
</output>
