---
phase: 01-foundation
type: execute
---

<objective>
Ship the branded shell, PWA baseline, and live free-tier deployment so Phase 1 ends as a real product foundation instead of a local-only scaffold.

Purpose: Turn the technical foundation into a pitch-ready shell that looks intentional on mobile, installs cleanly, and is deployed end-to-end with Postgres attached.
Output: Branded landing and stub shells, manifest and service-worker baseline, and a live deployment backed by the migrated database.
</objective>

<execution_context>
~/.codex/get-shit-done/workflows/execute-phase.md
~/.codex/get-shit-done/templates/summary.md
~/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
Read file: .planning/PROJECT.md
Read file: .planning/ROADMAP.md
Read file: .planning/phases/01-foundation/01-CONTEXT.md
Read file: .planning/phases/01-foundation/01-RESEARCH.md
Read file: package.json
Read file: next.config.ts
Read file: app/layout.tsx
Read file: app/page.tsx
Read file: app/globals.css
Read file: db/client.ts
Read file: db/schema/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the branded mobile-first shell and role pivot routes</name>
  <files>app/layout.tsx, app/page.tsx, app/(consumer)/page.tsx, app/(business)/page.tsx, app/globals.css, components/brand/, components/nav/, components/shell/</files>
  <action>Create the product shell described in the phase context: a polished landing page with a single role pivot, a consumer stub shell, a business stub shell, and shared layout primitives that already feel like a live marketplace product on phone-sized screens. Use a bold commerce-first visual direction, rounded surfaces, strong spacing, and a bottom-nav pattern for mobile. Keep routes server-first and use Client Components only for genuinely interactive islands. Avoid generic SaaS aesthetics, placeholder lorem ipsum, or a desktop-first layout, because this phase is explicitly about a product-feeling mobile shell.</action>
  <verify>`npm run build` passes and the core routes render successfully in a local production start</verify>
  <done>The app has a coherent branded shell with separate consumer and business stubs and no visual trace of a starter template</done>
</task>

<task type="auto">
  <name>Task 2: Add the minimal PWA baseline and deployment-safe service-worker behavior</name>
  <files>app/manifest.ts, public/icons/, public/sw.js, components/pwa/, next.config.ts, app/layout.tsx</files>
  <action>Add the PWA baseline recommended by the research: manifest, installable metadata, icon set, theme colors, any small install guidance needed for iOS, and a conservative service worker served with non-stale headers. Keep the service worker intentionally minimal and safe for frequent deploys; cache basics only if they do not create stale asset risk. Do not introduce plugin-driven offline infrastructure or aggressive navigation caching, because this phase only needs installability and a clean update path.</action>
  <verify>`npm run build` passes and the service-worker asset is served with the intended headers in a local production run</verify>
  <done>The application satisfies the practical PWA baseline for installability without taking on brittle offline complexity</done>
</task>

<task type="auto">
  <name>Task 3: Deploy the app end-to-end on the chosen free-tier stack and run the initial hosted migration</name>
  <files>package.json, .env.example, drizzle.config.ts, db/client.ts</files>
  <action>Use the chosen stack from research: Vercel for hosting and Neon for Postgres. Link or create the Vercel project, attach the database integration or otherwise wire the hosted `DATABASE_URL`, deploy the app, and run the committed migration workflow against the hosted database. Capture the live URL and verify the deployed app answers successfully. If a CLI or provider auth gate blocks automation, stop only at the minimal necessary human-auth checkpoint rather than switching to an undocumented manual workaround. Do not deploy as a static export and do not bypass the checked-in migration workflow.</action>
  <verify>The hosted URL returns 200 and the hosted database reflects the committed initial migration</verify>
  <done>The app is live on the free-tier stack with the schema applied and the deployment path proven end-to-end</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>A live branded shell with consumer and business stubs, PWA install metadata, and the hosted database-backed deployment</what-built>
  <how-to-verify>
    1. Visit the live URL on a phone-sized browser.
    2. Confirm the landing page reads like a real product rather than a starter template.
    3. Tap both role pivots and confirm the consumer and business shells each render with the shared branded layout.
    4. Open the browser install/add-to-home-screen flow and confirm the app name, icon, and standalone presentation are correct.
    5. Confirm there are no obvious visual regressions, broken routes, or shell-level errors.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe the issues that need to be corrected</resume-signal>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `npm run build` passes
- [ ] The landing, consumer, and business routes render locally and in production
- [ ] Manifest, icons, and service worker are present and correctly served
- [ ] The live deployment responds successfully
- [ ] Hosted Postgres has the committed initial schema applied
</verification>

<success_criteria>

- The phase ships as a real branded mobile-first shell
- The app is installable as a PWA baseline without brittle offline behavior
- The Vercel + Neon deployment path is proven end-to-end
- The user can review the live result on a phone and approve it
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`:

# Phase 1 Plan 3: Shell and Deploy Summary

**Shipped the branded PWA shell and proved the live free-tier deployment path.**

## Accomplishments

- Built the mobile-first landing page and role shells
- Added the installable PWA baseline
- Deployed the app live with the migrated database attached

## Files Created/Modified

- List the shell, PWA, and deployment-relevant files

## Decisions Made

- Record final brand and implementation choices that matter to later phases

## Issues Encountered

- Record deployment, installability, or UX issues and how they were resolved, or "None"

## Next Step

Phase complete, ready for the next phase
</output>
